# AI_product

## Getting started

```bash
git clone https://github.com/OYerokhina/AI_product.git
cd AI_product
```

## Tooling

- **Playwright MCP** — browser automation (`claude mcp`); artifacts land in `.playwright-mcp/` and are ignored.
